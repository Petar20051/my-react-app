import type {CardType} from '../types/cards';

export const CARD_TYPE: Record<CardType, CardType> = {
	podcasts: 'podcasts',
	news: 'news',
	events: 'events',
	featured: 'featured',
	solutions: 'solutions',
};
